import React from "react"

import SocialCard from "./molecules/SocialCard"

export default () => (
    <footer style={{ backgroundColor: `#fff`, textAlign: `center`, padding: `0.8em`}}>
      <SocialCard />
      <small>All the content in this website are licensed under the CC-BY-SA license, unless otherwise specified</small>
    </footer>
  )
